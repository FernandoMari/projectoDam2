extends Node2D


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	#var new_entity = load("res://Scenes/Main.tscn").instance()
	# Puedes personalizar la posición de la nueva entidad
	#new_entity.position = Vector2(100, 100)
	# Agrega la nueva entidad a la escena
	#get_node("MessageContainer").add_child(new_entity)
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
